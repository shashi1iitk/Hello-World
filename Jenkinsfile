pipeline {
    agent any

    stages {
        stage('Start') {
            steps {
                echo 'Starting the pipeline'
            }
        }
        stage('Finish') {
            steps {
                echo 'Finishing the pipeline'
            }
        }
    }
}
